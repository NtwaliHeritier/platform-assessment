grafana:
  enabled: true

  adminUser: admin
  adminPassword: prom-operator

  service:
    type: ClusterIP

  persistence:
    enabled: false

  sidecar:
    dashboards:
      enabled: true
    datasources:
      enabled: true