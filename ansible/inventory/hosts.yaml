all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
  hosts:
    local-vm:
      host_ip: "{{ lookup('env','MULTIPASS_IP') }}"

webservers:
  hosts:
    local-vm: {}
    
monitoring:
  hosts:
    local-vm: {}