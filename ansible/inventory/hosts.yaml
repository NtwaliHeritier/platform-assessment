all:
  vars:
    ansible_user: ubuntu
    ansible_port: 2222
    ansible_ssh_private_key_file: ~/.ssh/id_rsa

  hosts:
    docker-target:
      ansible_host: 127.0.0.1

webservers:
  hosts:
    docker-target:

monitoring:
  hosts:
    docker-target: